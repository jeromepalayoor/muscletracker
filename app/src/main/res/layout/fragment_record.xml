<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btnStartWorkout"
        android:layout_margin="14dp"
        android:padding="10dp"
        android:text="@string/start_new_workout_session_text"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/pastSessionText"
        android:textSize="16sp"
        android:layout_marginHorizontal="14dp"
        android:padding="10dp"
        android:textColor="?android:attr/textColorSecondary"
        android:text="@string/past_session_text"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/noPastSessionText"
        android:visibility="gone"
        android:textSize="24sp"
        android:layout_marginHorizontal="14dp"
        android:gravity="center"
        android:padding="20dp"
        android:textColor="?android:attr/textColorPrimary"
        android:text="@string/no_sessions_recorded_yet_text"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerSessions"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginHorizontal="14dp"
        android:clipToPadding="false"/>
</LinearLayout>